{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all windows",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "core:event:default"
  ]
}
